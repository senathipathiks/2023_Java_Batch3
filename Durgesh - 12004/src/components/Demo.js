import React from 'react'

function Demo(a,b) {
  return (
    <div>
      const add = a+b;
      const subtract = a-b;
      const multiply = a*b;
      const divide = a/b

    </div>
  )
}

export default Demo
